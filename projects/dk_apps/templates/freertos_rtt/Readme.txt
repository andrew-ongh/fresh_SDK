Project folder is the location for start building a new project.
A template main.c file is provided.
The main.c creates a template task which increments a counter every 200 ms.  

This project redirects I/O (e.g. printf) to SEGGER's Real Time Terminal.
To access the terminal, open a telnet terminal to locahost:19021.
